mixin info-box(options)
    section(class="info-box")
        div(class="info-box__image" )
            img(src=options.imagePath)
        header(class="info-box__header")
            span(class="info-box__title")= options.header
            span(class="info-box__text")= options.caption